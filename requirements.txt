# Core framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Frontend
streamlit>=1.35.0
requests>=2.31.0

# Web scraping
beautifulsoup4>=4.12.0

# LangChain ecosystem — pin the family together to avoid cross-version breakage.
# BUG FIX: langchain-chroma was missing (required by vector_store.py).
# BUG FIX: langchain-huggingface was missing (correct import for HuggingFaceEmbeddings;
#           langchain_community.embeddings.HuggingFaceEmbeddings is deprecated).
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.2.0
langchain-chroma>=0.1.0
langchain-huggingface>=0.0.3

# Vector DB
chromadb>=0.5.0

# Embeddings
sentence-transformers>=3.0.0

# OpenAI
openai>=1.30.0

# Config / env
python-dotenv>=1.0.0
pydantic>=2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
